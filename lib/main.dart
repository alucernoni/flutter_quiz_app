import 'package:flutter/material.dart';
import 'package:quiz_app/home_container.dart';

void main() {
  runApp(
    const MaterialApp(
      // image
      // text box
      // button
      // all centered in a column? in a container?
      home: Scaffold(
        body: HomeContainer()
      )
    ),
  );
}
